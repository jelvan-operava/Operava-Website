interface TypewriterHeroProps {
  fullText?: string
  className?: string
}

/** Hero typewriter line removed — component kept as no-op so existing imports do not break. */
export default function TypewriterHero(_props: TypewriterHeroProps) {
  return null
}
